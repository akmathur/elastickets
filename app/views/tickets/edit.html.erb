<h4>Edit Ticket# <%= @ticket.id %></h4>
<%= form_for @ticket do |f| %>
  <%= render "form", f: f %>
  <%= f.submit "Update", class: "button-primary" %> or <%= link_to "cancel", tickets_path %>
<% end %>
