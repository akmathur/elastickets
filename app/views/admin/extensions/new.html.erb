<h4>Extensions - add new</h4>
<%= form_for [:admin, @extension] do |f| %>
  <%= render 'form', f: f %>
  <%= f.submit "Save", class: "button-primary" %> or <%= link_to "cancel", admin_path %>
<% end %>
